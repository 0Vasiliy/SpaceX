//adaptive selector
jQuery(document).ready(function($) {
    $('.promo').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

jQuery(document).ready(function($){
    $('.header').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

jQuery(document).ready(function($){
    $('header__logo').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.menu').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.menu__list').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.menu__item').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

jQuery(document).ready(function($){
    $('.title__block').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.title__wrapper__market').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.title__wrapper__guarant').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.title__wrapper__calendar').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){
    $('.title__wrapper__journey').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

jQuery(document).ready(function($){
    $('.invite').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

jQuery(document).ready(function($){

    $('.invite__text').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});
jQuery(document).ready(function($){

    $('.container').selectorQuery({
        'widthStops': [320, 480, 640, 960, 1024, 1280], // An array of widths
        'classPrefix': 'max-' // Prefix class names with this
    });
});

    
    
    
   
   
    



